/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StorageSizeFormatterPipe } from './storage-size-formatter.pipe';

describe('StorageSizeFormatterPipe', () => {
  it('create an instance', () => {
    let pipe = new StorageSizeFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
